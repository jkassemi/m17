[package]
name = "core-types"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
arrow = "40.0"
parquet = { version = "40.0", features = ["arrow"] }
config = "0.13"
tokio = { version = "1.0", features = ["full"] }
