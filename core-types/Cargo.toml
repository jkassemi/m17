[package]
name = "core-types"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
arrow = "57.0"
parquet = { version = "57.0", features = ["arrow"] }
config = "0.15"
tokio = { version = "1.0", features = ["full"] }
