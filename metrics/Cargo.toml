[package]
name = "metrics"
version = "0.1.0"
edition = "2021"

[dependencies]
prometheus = "0.14"
tokio = { version = "1.0", features = ["full"] }
hyper = { version = "1.7", features = ["server", "http1"] }
