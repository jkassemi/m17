[package]
name = "rest-source"
version = "0.1.0"
edition = "2021"

[dependencies]
core-types = { path = "../core-types" }
tokio = { version = "1.0", features = ["full"] }
reqwest = { version = "0.11", features = ["json"] }
