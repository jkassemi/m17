[package]
name = "storage"
version = "0.1.0"
edition = "2021"

[dependencies]
core-types = { path = "../core-types" }
tokio = { version = "1.0", features = ["full"] }
parquet = { version = "57.0", features = ["arrow"] }
